config {
    type: 'incremental',
    schema: 'managed',
    name: 'MP_vendas_incremental',
    tags: ['inicial', 'incremental'],
    dependencies: ['MP_ext_vendas'],
}


SELECT *
FROM ${ref('MP_ext_vendas')} cl


${
    when(incremental(),
    `WHERE pedido_data > (SELECT MAX(pedido_data) FROM ${self()})`)
}
